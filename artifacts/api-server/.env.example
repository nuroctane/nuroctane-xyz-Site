# Database (optional — currently unused)
# DATABASE_URL=postgresql://...

# Upstash Redis KV — used for visitor books and modkeys configs
nuroctanesitestorage_KV_REST_API_URL=
nuroctanesitestorage_KV_REST_API_TOKEN=

# GitHub OAuth — for user sign-in on modkeys page
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# JWT — used for session cookies
JWT_SECRET=
