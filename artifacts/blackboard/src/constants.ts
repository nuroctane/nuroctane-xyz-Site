// ── World-space bounds ────────────────────────────────────────────────────────
// The camera path spans z≈28 (t=0) to z≈-182 (t=1).
// Geometry extends slightly beyond those limits for visual depth.
export const WORLD_Z_MIN = -190;
export const WORLD_Z_MAX = 28;
export const WORLD_X_HALF = 40;
